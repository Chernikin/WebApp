package com.chernikin.webapp;

import com.chernikin.webapp.domain.Company;
import com.chernikin.webapp.domain.Cpu;
import com.chernikin.webapp.domain.Gpu;
import com.chernikin.webapp.domain.Product;
import com.chernikin.webapp.service.ProductService;

/**
 * Hello world!
 */
public class App {
    public static void main(String[] args) {
        /*final ProductService productService = new ProductService();

//        final Product product = getProductTest(productService);
//        System.out.println(product);

//        createProductTest(productService);

        final Product product = productService.getProductById(1);
        product.setProductName("Updated productName");
        final Product updatedProduct = productService.updateProduct(product);
        System.out.println(updatedProduct);
    }

    private static Product getProductTest(ProductService productService) {
        return productService.getProductById(1);
    }

    private static void createProductTest(ProductService productService) {
        final Product newProduct = new Product();
        newProduct.setProductName("New notebook");
        newProduct.setPrice(1234);
        newProduct.setSpecification("New specification");

        final Company company = new Company();
        company.setId(1);
        newProduct.setCompany(company);

        final Cpu cpu = new Cpu();
        cpu.setId(1);
        newProduct.setCpu(cpu);

        final Gpu gpu = new Gpu();
        gpu.setId(1);
        newProduct.setGpu(gpu);

        productService.createProduct(newProduct);*/
    }
}
